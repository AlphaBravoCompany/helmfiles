bases:
- ../environments.yaml

templates:
  getVars: &getVars
    values:
    - "../charts/{{`{{ .Release.Name }}`}}/values.yaml.gotmpl"

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: nginxingress
    installed: {{ eq .Environment.Name "default" "uat" | toYaml }}
    namespace: abscan
    chart: bitnami/nginx-ingress-controller
    <<: *getVars
  - name: mongodb
    installed: {{ eq .Environment.Name "default" "uat" | toYaml }}
    namespace: abscan
    chart: bitnami/mongodb
    <<: *getVars
  - name: redis
    namespace: abscan
    chart: bitnami/redis
    <<: *getVars

helmDefaults:
  atomic: true
  cleanupOnFail: true
  createNamespace: true